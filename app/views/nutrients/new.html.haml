= render_flash
= simple_form_for @nutrient, html: { class: 'col-sm-5' } do |f|
  %legend New Nutrient
  = render 'fields', f: f
  = f.button :submit, 'Save', class: 'btn btn-primary'
  = link_to 'Back to Index', nutrients_path, class: 'btn btn-info'
